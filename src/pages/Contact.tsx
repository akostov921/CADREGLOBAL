import React, { useState } from "react";const Contact: React.FC = () => {  const [form, setForm] = useState({ name: "", email: "", phone: "", message: "" });  const onChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) =>    setForm((p) => ({ ...p, [e.target.name]: e.target.value }));  const handleSubmit = (e: React.FormEvent) => {    e.preventDefault();    const to = "contact@cadreglobal.net";    const subject = encodeURIComponent(`Website Contact — ${form.name || form.email}`);    const body = encodeURIComponent(      `Name: ${form.name}\nEmail: ${form.email}\nPhone: ${form.phone}\n\nMessage:\n${form.message}`    );    window.location.href = `mailto:${to}?subject=${subject}&body=${body}`;  };  return (    <main className="container mx-auto px-6 py-20 max-w-2xl">      <h1 className="text-3xl md:text-4xl font-playfair mb-4">Contact</h1>      <p className="text-muted-foreground mb-8">Use the form below to contact us directly.</p>      <form onSubmit={handleSubmit} className="space-y-4">        <input name="name" value={form.name} onChange={onChange} placeholder="Full name" required className="w-full rounded-md px-4 py-2 bg-input/60" />        <input name="email" value={form.email} onChange={onChange} placeholder="Email" type="email" required className="w-full rounded-md px-4 py-2 bg-input/60" />        <input name="phone" value={form.phone} onChange={onChange} placeholder="Phone" className="w-full rounded-md px-4 py-2 bg-input/60" />        <textarea name="message" value={form.message} onChange={onChange} placeholder="Message" rows={6} className="w-full rounded-md px-4 py-2 bg-input/60" />        <div className="flex items-center gap-3">          <button type="submit" className="bg-accent text-accent-foreground px-4 py-2 rounded-md">Send</button>          <button type="button" onClick={() => setForm({ name: "", email: "", phone: "", message: "" })} className="text-muted-foreground">Clear</button>        </div>      </form>    </main>  );};export default Contact;